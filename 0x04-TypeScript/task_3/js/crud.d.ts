import { RowID, RowElement } from './interface'

export  function insertRow(rowID: rowID): number;

export  function deleteRow(row: RowId): void;

export  function updateRow(rowId: RowID, row: RowElement): number;
